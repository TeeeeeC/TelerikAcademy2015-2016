@using BestSportsStore.Web.Models.Menu.Category

@model IEnumerable<SubCategoryViewModel>

@{
    ViewBag.Title = "Index";
    const string ControllerName = "SubCategories";
}

<div class="row text-center" style="margin-top: 30px">
    <div class="col-md-6">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <h3 class="panel-title">@ControllerName</h3>
            </div>
            <div class="panel panel-body">
                <table class="table table-striped table-hover">
                    <tr>
                        <th>@Html.ActionLink("Create new subCategory", "Create", ControllerName, new { }, new { @class = "btn btn-default" })</th>
                    </tr>
                    <tr>
                        <th class="text-center">Name</th>
                        <th></th>
                        <th></th>
                    </tr>

                    @foreach (var subcategory in Model)
                    {
                        <tr>
                            <td>@subcategory.Name</td>
                            <td>@Html.ActionLink("Edit", "Update", ControllerName, new { subCategoryId = subcategory.Id }, new { @class = "btn btn-default" })</td>
                            <td>@Html.ActionLink("Delete", "Delete", ControllerName, new { subCategoryId = subcategory.Id, url = Request.Url.PathAndQuery }, new { @class = "btn btn-default" })</td>
                        </tr>
                    }
                </table>
            </div>
        </div>
    </div>
</div>
