Follow this simple commands:
1. npm install - restore dependencies in project
2. npm start - start node js server at port 3000

Optional commands:
if you want to transform some jade files to html in views folder, open cmd in views folder and enter:
jade "fileName".jade